# Guidelines
- App is isntalled on two phones always.So any logic triggered automatcially(such as on ending of week) needs to be careful about not running twice.
- Always keep tracking and testibility in mind, we should always be able to query the phones local storage to figure out values and debug.